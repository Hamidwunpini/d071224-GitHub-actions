name: Build, Test and Deploy

on: 
  - push 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Cowsay Program
        run: sudo apt-get cowsay -y

      - name: Execute Cowsay Command 
        run: cowsay -f dragon "Run for cover, I am a DRAGON......RAWR" >> dragon.txt

      - name: Sleep for 30 seconds
        run: sleep 30


  test: 
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Sleep for 10 seconds
        run: sleep 10

      - name: Test file exists 
        run: grep -i "dragon" dragon.txt

  deploy: 
    needs: test
    runs-on: ubuntu-latest
    steps: 
      - name: Read File
        run: cat dragon.txt

      - name: Deploy 
        run: echo "Deploying......."


- We will run into another error. 
- Since the three jobs are running on three different machines, 
we need to find a way to move the built artifact across to test and deploy.
https://github.com/marketplace/actions/upload-a-build-artifact

eg. 7

name: Build, Test and Deploy

on: 
  - push 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Cowsay Program
        run: sudo apt-get install cowsay -y

      - name: Execute Cowsay Command 
        run: cowsay -f dragon "Run for cover, I am a DRAGON......RAWR" >> dragon.txt

      - name: Upload Dragon File
        uses: actions/upload-artifact@v4
        with:
          name: dragon-text-file
          path: dragon.txt


  test: 
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Dragon File
        uses: actions/download-artifact@v4
        with:
          name: dragon-text-file

      - name: Test file exists 
        run: grep -i "dragon" dragon.txt

  deploy: 
    needs: test
    runs-on: ubuntu-latest
    steps: 
      - name: Download Dragon File
        uses: actions/download-artifact@v4
        with:
          name: dragon-text-file

      - name: Read File
        run: cat dragon.txt

      - name: Deploy 
        run: echo "Deploying......."